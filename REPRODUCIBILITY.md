# 论文复现指南（原始数据 + 代码）

> 写给拿到数据包（`水池实验原始数据_20260916.zip`）和本仓库的协作者。
> 结论先行：**论文的每个图、每张表都能复现**；其中大部分逐点/逐格一致，
> 少数数字只能统计一致或存在已查明的历史差异（见 §1，都有原因，不是操作问题）。

## 0. 你手上有什么

- **数据包**：1437 个原始 wav（8 通道 / 16 bit / 64 kHz）+ 当年实验期输出的逐条件误差 xlsx
  （**论文 Table I/II、Fig.3 数字的直接出处**）+ 数据结构说明（包内 `README_数据说明.md`）
- **本仓库**：全部处理代码（`code/`）+ 我方重算结果（`results/`）

## 1. 一致性预期（先读这个，免得被吓到）

| 论文内容 | 复现方式 | 预期一致性 |
|---|---|---|
| Table I / Table II、Fig.3 的数字 | 直接读数据包内 xlsx | **完全一致**（就是数字出处） |
| Fig.2 定位图谱 | `fig2.py` / `avg_maps.py` 重算 | **逐点一致**（确定性管线） |
| Fig.4 SNR 曲线 | `run_e2_snr.py` + `fig4_snr.mjs` | **逐点一致**（噪声种子固定：crc32(条件)+事件号+SNR） |
| Table I/II **从 wav 重算** | `run_315_tone.py` | 分三档，见下 |

从 wav 重算（E1）的三档结果：

1. **精确复现**：5000Hz_3 的 CBF 与 MVDR（20/20 事件、4 位小数全同）；
   7000Hz_1、9000Hz_1 的 CBF（20/20）。
2. **统计一致**：MUSIC / CSDM-MUSIC 全部条件，以及其余多数条件——均值接近，
   逐事件数值不重合。MUSIC 族是**数学性质**：单快拍秩-1 CSDM 的噪声子空间在
   简并特征簇内基任意，LAPACK/MATLAB 返回不同但都合法的正交基，跨机器必然不同。
   **不要追逐事件级相等，比均值即可。**
3. **已知历史差异（论文以 xlsx 为准）**：MVDR 在 (7000Hz,1m)、(9000Hz,1m) 两格——
   重算得 0.09–0.14 m，当年归档是 2.27–2.42 m；同参数下 CBF 却逐格精确，说明
   归档 MATLAB 的 MVDR 在这两条件行为异常，属存档无法解释的分歧。这两格论文
   本来就在"退化区"，不影响结论。

## 2. 环境

Python ≥ 3.8，第三方库只要两个：

```bash
pip install numpy matplotlib
```

（其余全是标准库；wav 用 `wave` 读，xlsx 解析在 `dump_xlsx.py` 里用 stdlib zipfile 完成。）

## 3. 复现步骤

```bash
# 1) 定标：逐条件拟合几何尺度 s（读数据包 xlsx）→ val_hist8_report.json
python val_hist8.py <数据包路径>

# 2) E1 逐事件定位误差 → e1_errors.csv（对照 Table I/II / Fig.3）
python run_315_tone.py <数据包路径>

# 3) E2 注噪 SNR 扫描 → e2_snr.csv（Fig.4 数据）
python run_e2_snr.py <数据包路径>

# 4) 出图：Fig.2 用 fig2.py；Fig.4 用 node fig4_snr.mjs e2_snr.csv <outdir>
```

`results/` 与论文的对应：`fig2/`→Fig.2，`fig4/`→Fig.4，`e1_errors.csv`/`e2_snr.csv`
→表格与曲线数据源。

## 4. 待修 bug（就一个，留给后来者）

**`run_315_tone.py` 的事件收集是非递归 glob**：只扫条件目录顶层，而
`7000Hz 2`、`7000Hz 3`、`13000Hz 3`（以及 `5000Hz 2`）的大部分 wav 在**子文件夹**里。

- 症状：`results/e1_v3_report.json` 中 5000Hz_2 / 7000Hz_2 / 7000Hz_3 的 n=1、
  13000Hz_3 无记录。**数据本身都在**（递归清点：21 / 21 / 62 个，均为 8 通道有效格式），
  是脚本没读进去。
- 修法：把事件收集改成 `os.walk` / `pathlib.Path.rglob` 递归取 `*.wav`（跳过 xlsx），
  重跑 E1，并与数据包内当年逐事件表对照确认口径。
- 连带可做：修复后 `val_hist8` 对这几条件的定标可复核；E2 想补 7000Hz_3 面板也可以。
- 13000Hz_3 当年真值表本就不含 13 kHz——重跑出的 62 个 wav 如何取舍，由你们定。

## 5. 口径备注

- E2 / Fig.4 每条件用 20 个有效事件；论文面板条件为 5000Hz_3、3000Hz_2、11000Hz_1。
- 所有论文数字以当年 xlsx（=数据包内文件）为准；重算结果作复现性验证用。

---
2026-09-16 整理。有问题开 GitHub issue 即可。
